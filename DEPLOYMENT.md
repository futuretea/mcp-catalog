# MCP Server 部署分类

## 离线 (Offline) - 1 个

纯本地运行，无需网络：

- MarkItDown (markitdown.yaml)

## 内网 (Intranet) - 20 个

可配置连接内网服务（如私有数据库、内网服务）：

- AntV Charts (antv_charts.yaml)
- BigQuery Toolbox (bigquery.yaml)
- Chroma Cloud (chroma.yaml)
- Context7 (context7.yaml)
- Elasticsearch (elasticsearch.yaml)
- Files.com (filescom.yaml)
- Google Cloud Run (cloud_run.yaml)
- Grafana (grafana.yaml)
- HubSpot (hubspot.yaml)
- MongoDB Atlas Management (mongodb_atlas_management.yaml)
- MongoDB Database (mongodb_database.yaml)
- MySQL (mysql.yaml)
- PagerDuty (pagerduty.yaml)
- PostgreSQL (postgresql.yaml)
- Redis (redis.yaml)
- Salesforce (salesforce.yaml)
- Snowflake (snowflake.yaml)
- Supabase (supabase.yaml)
- Tableau (tableau.yaml)
- Terraform (terraform.yaml)

## 公网 (Public) - 57 个

必须连接公网服务：

- Asana (asana.yaml)
- Atlassian (atlassian.yaml)
- AWS API (aws.yaml)
- AWS CDK (aws_cdk.yaml)
- AWS Documentation (aws_documentation.yaml)
- AWS EKS (aws_eks.yaml)
- AWS Kendra (aws_kendra.yaml)
- AWS Knowledge (aws_knowledge.yaml)
- AWS Redshift (aws_redshift.yaml)
- Azure (azure.yaml)
- Brave Search (brave_search.yaml)
- Browserbase (browserbase.yaml)
- Calendar (calendar.yaml)
- Cloudflare (cloudflare.yaml)
- Contact (contact.yaml)
- Databricks Genie Spaces (databricks_genie.yaml)
- Databricks Unity Catalog Functions (databricks_uc_functions.yaml)
- Databricks Vector Search (databricks_vector_search.yaml)
- Datadog (datadog.yaml)
- DeepWiki (deepwiki.yaml)
- DigitalOcean (digitalocean.yaml)
- DuckDuckGo Search (duckduckgo_search.yaml)
- Dynatrace (dynatrace.yaml)
- Exa Search (exa_search.yaml)
- Excel (excel.yaml)
- Firecrawl (firecrawl.yaml)
- GitHub (github.yaml)
- GitHub Enterprise (github_enterprise.yaml)
- GitLab (gitlab.yaml)
- GitMCP (gitmcp.yaml)
- Gmail (gmail.yaml)
- Google Calendar (google-calendar.yaml)
- Google Drive (google-drive.yaml)
- Google Maps Grounding Lite (google-maps-grounding-lite.yaml)
- Google Sheets (google-sheets.yaml)
- Linear (linear.yaml)
- Microsoft Learn (microsoft-docs.yaml)
- Monday.com (mondaycom.yaml)
- Morningstar (morningstar.yaml)
- Neon Database (neon.yaml)
- Notion (notion.yaml)
- OneDrive (onedrive.yaml)
- Outlook (outlook.yaml)
- PayPal (paypal.yaml)
- Playwright (playwright.yaml)
- Postman (postman.yaml)
- Ref (ref.yaml)
- Render (render.yaml)
- Slack (slack.yaml)
- Square (square.yaml)
- Stripe (stripe.yaml)
- Tavily Search (tavily_search.yaml)
- Todoist (todoist.yaml)
- Wix (wix.yaml)
- Word (word.yaml)
- WordPress (wordpress.yaml)
- Zapier (zapier.yaml)

---

总计: 78 个
